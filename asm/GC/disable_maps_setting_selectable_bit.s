# Disable maps setting the "Selectable" bit for their minimap when loaded.
# This is to prevent a realm from being accessible with the realm teleporter
# before the client sets the access for it.
nop
